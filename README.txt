Water Operator Flashcards PWA

This is an installable Progressive Web App. It must be hosted over HTTPS (for example GitHub Pages) for Android Chrome to show Install app. Upload all files in this folder together. Then open the HTTPS URL in Chrome and choose Install app / Add to Home screen. Offline use works after the first hosted load.
